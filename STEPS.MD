# How to use Design OS with Claude Code?

1. Install Antigravity Claude Proxy if not already done
`bash
npm install -g antigravity-claude-proxy@latest
`
2. Run it using 
`bash 
antigravity-claude-proxy start
`